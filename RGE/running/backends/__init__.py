"""Production backends for specific intermediate-EFT field contents.

The generic pipeline dispatches here by physical active-field content.  Backend
modules may retain historical implementation names internally when required for
compatibility, but those names must not leak into pipeline orchestration.
"""
